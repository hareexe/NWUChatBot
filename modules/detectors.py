def compute_detectors(user_tokens: set):
    # Return a dict of all detector flags currently computed in app.py
    # Example:
    detectors = {}
    # ...existing detector computations moved from get_semantic_response_debug up to aliases & strong_*...
    # detectors["early_greet"] = early_greet
    # detectors["strong_univ_status"] = strong_univ_status
    # detectors["strong_founders_establish_list"] = strong_founders_establish_list
    # detectors["strong_first_college_president"] = strong_first_college_president
    # ...plus all is_* flags referenced downstream...
    return detectors
